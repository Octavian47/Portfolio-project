import React from 'react';
import './Layout.css';  // Import the shared CSS file for layout and header

function Header() {
  return (
    <header className="App-header">
      <h1>My Portfolio</h1>
    </header>
  );
}

export default Header;
